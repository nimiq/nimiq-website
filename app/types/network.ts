// enums are not only types but also values in TypeScript, so file needs to be .ts and not .d.ts

export enum NetworkStatus {
  CONNECTING = 'connecting',
  SYNCING = 'syncing',
  ESTABLISHED = 'established',
}
